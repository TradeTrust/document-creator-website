import React from "react";
import { PublishLoader } from "./PublishLoader";

export default {
  title: "Publish Loader|Publish Loader",
  component: PublishLoader,
  paremeters: {
    info: { inline: true, header: false },
  },
};

export const Default = () => <PublishLoader />;
