import { DynamicFormLayout } from "./DynamicFormLayout";

describe("DynamicFormLayout", () => {
  xit("should render the progress bar", () => {});
  xit("should render the back button", () => {});
  xit("should unset activeFormIndex on clicking back", () => {});
  xit("should render toggle for preview mode", () => {});
  xit("should render the active form", () => {});
});
