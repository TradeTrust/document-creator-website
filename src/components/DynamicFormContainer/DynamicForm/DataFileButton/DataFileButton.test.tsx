import { DataFileButton } from "./DataFileButton";

describe("DataFileButton", () => {
  xit("should render correctly", () => {});

  // Can try https://github.com/react-dropzone/react-dropzone#testing
  xit("should fire onDataFile when a file is successfully read", () => {});
  xit("should show error when a file cannot be read", () => {});
});
