import { CustomObjectFieldTemplate } from "./CustomObjectField";

describe("CustomObjectFieldTemplate", () => {
  xit("should render all fields correctly", () => {});
  xit("should render title if it exist", () => {});
  xit("should render all properties in a list", () => {});
});
