import {} from "./ToggleSwitch";

describe("ToggleSwitch", () => {
  xit("should fire handleToggle when clicked", () => {});
  xit("should display `ON` when isOn is true", () => {});
  xit("should display `OFF` when isOn is false", () => {});
});
